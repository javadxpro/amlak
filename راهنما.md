# 🏠 راهنمای سایت املاک جواد حامدی

## فایل‌ها

| فایل | کاربرد |
|---|---|
| `index.html` | کل سایت |
| `properties.json` | **لیست ملک‌ها — فقط همین فایل را برای تغییر املاک ویرایش کنید** |
| `.nojekyll` | لازم است روی گیت‌هاب بماند (تا گیت‌هاب سایت را دستکاری نکند) |
| `sync.py` | اختیاری — برای تست روی کامپیوتر (نیاز به پایتون) |

## 🔁 آپدیت کردن سایت روی گیت‌هاب (مرحله به مرحله)

1. برو به صفحه ریپازیتوری‌ات در github.com
2. بالای لیست فایل‌ها روی **Add file** بزن → **Upload files**
   (یا فایل‌ها را مستقیم بکش و داخل صفحه مرورگر رها کن)
3. فایل‌های `index.html` و `properties.json` جدید را بکش و رها کن
4. پایین صفحه روی **Commit changes** بزن
5. صبر کن ۱ تا ۲ دقیقه تا گیت‌هاب سایت را دوباره بسازد (پیشرفتش در تب **Actions** دیده می‌شود)
6. سایت را باز کن و با **Ctrl+Shift+R** تازه‌سازی کن (تا نسخه کش‌شده قبلی نیاید)

## ⚠️ چرا بعضی وقت‌ها لیست و فیلترها نمی‌آمد؟

سه دلیل داشت که هر سه حل شده:

1. **کش:** مرورگر یا گیت‌هاب نسخه قدیمی فایل را نشان می‌داد (mix نسخه قدیم و جدید).
   → حالا سایت موقع خواندن `properties.json` خودش کش را دور می‌زند؛ فقط بعد از آپلود جدید، صفحه را با Ctrl+Shift+R باز کن.
2. **JSON خراب:** اگر موقع ویرایش، `properties.json` یا بخش داخل `index.html` خراب می‌شد، کل اسکریپت بی‌صدا می‌مرد و هیچ‌ی نمایش داده نمی‌شد.
   → حالا اگر JSON خراب باشد، سایت به جایش **پیام خطای قرمز واضح** نشان می‌دهد و بقیه سایت کار می‌کند.
3. **دخالت Jekyll گیت‌هاب:** فایل `.nojekyll` را در ریپو نگه دار تا گیت‌هاب کاری با فایل‌ها نداشته باشد.

## فرمت هر ملک در properties.json

```json
{
  "id": 1,                          // شماره یکتا (عدد)
  "title": "آپارتمان ۸۵ متری کوی دانیال",   // عنوان آگهی
  "neighborhood": "کوی دانیال، بلوار کشاورز", // محل ملک
  "deal": "sale",                   // "sale" = فروش ، "rent" = رهن و اجاره
  "type": "apartment",              // apartment / villa / land / shop
  "area": 85,                       // متراژ
  "rooms": 2,                       // تعداد خواب (زمین و مغازه: 0)
  "floor": "۲ از ۴",                // طبقه (متن آزاد؛ برای زمین null)
  "buildYear": 1398,                // سال ساخت (عدد؛ ندارید null)
  "price": 680000000,               // قیمت کل فروش به تومان (فقط sale)
  "pricePerMeter": 8000000,         // قیمت هر متر (اختیاری، 0 = نمایش نمی‌دهد)
  "deposit": 100000000,             // ودیعه به تومان (فقط rent)
  "rent": 3000000,                  // اجاره ماهانه (0 = رهن کامل)
  "inAlley": true,                  // ✅ true = داخل کوچه ، false = برِ خیابان اصلی
  "parking": true,
  "elevator": false,
  "storage": true,
  "image": "logo",                  // "logo" = لوگوی سایت
  "description": "توضیحات آگهی…",    // متن سمت راست کارت
  "features": ["امکان ۱", "امکان ۲"]
}
```

### نکته‌ها

- فیلتر «داخل کوچه / برِ خیابان» از `inAlley` می‌خواند.
- فیلتر قیمت برای فروشی‌ها روی `price` و برای اجاره‌ای‌ها روی `deposit` اعمال می‌شود.
- اعداد فارسی هم قبول است (`"area": "۸۵"` درست کار می‌کند).
- ملک جدید = یک بلوک `{ ... }` جدید با `id` یکتا.
- **بعد از هر ویرایش JSON، از سایت تست بگیر**؛ اگر پیام قرمز دیدی یعنی JSON خراب شده (معمولاً یک کاما یا گیومه اضافه/کم).
- قیمت‌های فعلی **نمونه** است؛ با ملک‌های واقعی جایگزین کن.
