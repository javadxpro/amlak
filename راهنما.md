# 🏠 راهنمای سایت املاک جواد حامدی

## فایل‌ها

| فایل | کاربرد |
|---|---|
| `index.html` | کل سایت (لوگو، فاویکون و آیکون‌ها همه SVG داخل همین فایل هستند) |
| `properties.json` | **لیست ملک‌ها — فقط همین فایل را برای تغییر املاک ویرایش کنید** |
| `.nojekyll` | فایل خالی — لازم است روی گیت‌هاب بماند (تا گیت‌هاب سایت را دستکاری نکند) |
| `README.md` | معرفی پروژه و مراحل آپدیت |
| `server.js` / `sync.py` | اختیاری — برای تست روی کامپیوتر |

## 🔁 آپدیت کردن سایت روی گیت‌هاب (مرحله به مرحله)

1. برو به صفحه‌ی ریپازیتوری در github.com
2. فایل `properties.json` را باز کن و روی آیکون مداد (Edit this file) بزن؛ یا از **Add file ← Upload files** فایل جدید را بکش و رها کن
3. تغییرات را بده و پایین صفحه روی **Commit changes** بزن
4. ۱ تا ۲ دقیقه صبر کن تا گیت‌هاب سایت را دوباره بسازد (پیشرفتش در تب **Actions** دیده می‌شود)
5. سایت را باز کن و با **Ctrl+Shift+R** تازه‌سازی کن (تا نسخه‌ی کش‌شده‌ی قبلی نیاید)

## ⚠️ چرا قبلاً بعضی وقت‌ها لیست و فیلترها نمی‌آمد؟

سه دلیل داشت که هر سه حل شده:

1. **کش:** مرورگر یا گیت‌هاب نسخه‌ی قدیمی فایل را نشان می‌داد.
   → حالا سایت موقع خواندن `properties.json` خودش کش را دور می‌زند؛ فقط بعد از آپلود جدید، صفحه را با Ctrl+Shift+R باز کن.
2. **JSON خراب:** اگر موقع ویرایش، `properties.json` خراب می‌شد، کل اسکریپت بی‌صدا می‌مرد و هیچ‌چیز نمایش داده نمی‌شد.
   → حالا اگر JSON خراب باشد، سایت **پیام خطای قرمز واضح** نشان می‌دهد و با داده‌های پشتیبان به کار ادامه می‌دهد.
3. **دخالت Jekyll گیت‌هاب:** فایل `.nojekyll` را در ریپو نگه دار تا گیت‌هاب کاری با فایل‌ها نداشته باشد.

## 🖼️ تصاویر

هیچ فایل عکس (jpg / png / webp) در ریپو نگذار. برای هر ملک مقدار `"image": "logo"` را بگذار تا لوگوی SVG سایت
سمت چپ کارت نمایش داده شود. (اگر روزی خواستی عکس واقعی بگذاری، باید آدرس کامل اینترنتی عکس را در همین فیلد بنویسی؛
فایل عکس داخل ریپو قرار نمی‌گیرد.)

## فرمت هر ملک در properties.json

هر ملک دقیقاً همین ۲۱ فیلد را دارد (فیلد اضافه یا کم نکن) — فیلد جدید `shenazh` برای شناژبندی:

```json
{
  "id": 1,
  "title": "آپارتمان ۸۵ متری کوی دانیال",
  "neighborhood": "کوی دانیال، بلوار کشاورز",
  "deal": "sale",
  "type": "apartment",
  "area": 85,
  "rooms": 2,
  "floor": "۲ از ۴",
  "buildYear": 1398,
  "price": 680000000,
  "pricePerMeter": 8000000,
  "deposit": 0,
  "rent": 0,
  "inAlley": false,
  "parking": true,
  "elevator": false,
  "storage": true,
  "shenazh": true,
  "image": "logo",
  "description": "توضیحات آگهی…",
  "features": ["امکان ۱", "امکان ۲"]
}
```

| فیلد | معنی |
|---|---|
| `id` | شماره‌ی یکتا (عدد) — در پیام واتس‌اپ به‌عنوان «کد ملک» می‌آید |
| `title` | عنوان آگهی |
| `neighborhood` | محل ملک |
| `deal` | `"sale"` = فروش ، `"rent"` = رهن و اجاره |
| `type` | `apartment` آپارتمان / `villa` ویلا و خانه / `land` زمین / `shop` مغازه |
| `area` | متراژ (متر) |
| `rooms` | تعداد خواب (زمین و مغازه: `0`) |
| `floor` | طبقه — متن آزاد؛ برای زمین `null` |
| `buildYear` | سال ساخت (عدد شمسی)؛ اگر نداری `null` — سال ۱۴۰۳ به بعد بج «نوساز» می‌گیرد |
| `price` | قیمت کل فروش به تومان (فقط برای `sale`؛ برای اجاره `0`) |
| `pricePerMeter` | قیمت هر متر به تومان (اختیاری؛ `0` = نمایش داده نمی‌شود) |
| `deposit` | ودیعه/رهن به تومان (فقط برای `rent`؛ برای فروش `0`) |
| `rent` | اجاره‌ی ماهانه به تومان (`0` = رهن کامل) |
| `inAlley` | `true` = داخل کوچه ، `false` = برِ خیابان اصلی (فیلتر «موقعیت» از همین می‌خواند) |
| `parking` / `elevator` / `storage` / `shenazh` | `true` = دارد ، `false` = ندارد — `shenazh` یعنی شناژبندی (فیلتر و بج جدید) |
| `image` | `"logo"` = لوگوی SVG سایت |
| `description` | متن آگهی (در کارت حداکثر دو خط، در صفحه‌ی جزئیات کامل) |
| `features` | لیست امکانات — هر کدام داخل گیومه و با کاما جدا |

### نکته‌ها

- فیلتر قیمت برای فروشی‌ها روی `price` و برای اجاره‌ای‌ها روی `deposit` اعمال می‌شود (واحد ورودی کاربر: میلیون تومان).
- همه‌ی قیمت‌ها به **تومان** و به‌صورت عدد کامل نوشته می‌شوند (مثلاً ۶۸۰ میلیون = `680000000`)؛ سایت خودش «۶۸۰ میلیون» یا «۱٫۲ میلیارد» نمایش می‌دهد.
- اعداد فارسی هم قبول است (`"area": "۸۵"` درست کار می‌کند).
- ملک جدید = یک بلوک `{ ... }` جدید با `id` یکتا؛ بین بلوک‌ها کاما بگذار و بعد از آخرین بلوک کاما نگذار.
- **بعد از هر ویرایش JSON، از سایت تست بگیر**؛ اگر پیام قرمز دیدی یعنی JSON خراب شده (معمولاً یک کاما یا گیومه اضافه/کم).
- قیمت‌های فعلی **نمونه** است؛ با ملک‌های واقعی جایگزین کن.
